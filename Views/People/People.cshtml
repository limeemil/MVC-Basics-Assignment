@model CreatePersonViewModel

@{
    ViewData["Title"] = "People Index";
}

<h1>People Index</h1>

<div>
    <form asp-controller="People" asp-action="Search">
        <label asp-for="Search"></label><br />
        <input asp-for="Search" name="searchString"/><br />
        <input type="submit" value="Search" /><br />
    </form>
  
    <form asp-controller="People" asp-action="CreatePerson" method="post">
        <label asp-for="FirstName"></label><br />
        <input asp-for="FirstName" /><br />
        <label asp-for="LastName"></label><br />
        <input asp-for="LastName" /><br />
        <label asp-for="PhoneNumber"></label><br />
        <input asp-for="PhoneNumber" /><br />
        <label asp-for="City"></label><br />
        <input asp-for="City" /><br />
        <input type="submit" value="Create person" /><br />
    </form>

    <table class="table">
        <thead>
            <tr>
                @*<th>First Name</th>
        <th>Last Name</th>
        <th>Phone Number</th>
        <th>City</th>*@
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.people)
            {
            <tr>
                @*<td>@item.FirstName</td>
        <td>@item.LastName</td>
        <td>@item.PhoneNumber</td>
        <td>@item.City</td>
        <td><input type="button" id="ButtonDelete" value="Delete" onclick="location.href='@Url.Action("DeletePerson", "People", new { id = item.ID})'" /></td>*@
                <td><partial name="_PersonPartial" model="item" /></td>
                <td><input type="button" id="ButtonDelete" value="Delete" onclick="location.href='@Url.Action("DeletePerson", "People", new { id = item.ID})'" /></td>
            </tr>
            }
            
        </tbody>
    </table>
</div>